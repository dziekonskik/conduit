import { render, screen } from "@testing-library/react";
import { ImagesTest } from "./ImagesTest";

// describe("when rendered with a `name` prop", () => {
//   it("should paste it into the greetings text", () => {
//     render(<ImagesTest props="Test Name" />);
//     expect(screen.getByText(/Hello Test Name!/)).toBeDefined();
//   });
// });
it("runs the first test", () => {
  expect(true).toBe(true);
});
